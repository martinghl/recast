[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "focal"
version = "0.2.0"
description = "Foundation-model Contrastive Attribution: genes that define a target-vs-reference cell state."
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
license-files = ["LICENSE"]
authors = [{ name = "The FOCAL authors" }]
keywords = ["single-cell", "scRNA-seq", "attribution", "integrated-gradients", "foundation-model",
            "contrastive", "cell-state", "marker-genes", "bioinformatics"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Intended Audience :: Science/Research",
  "Programming Language :: Python :: 3",
  "Topic :: Scientific/Engineering :: Bio-Informatics",
]
dependencies = ["numpy", "scipy", "pandas", "anndata"]

[project.optional-dependencies]
attribution = ["torch", "captum", "scimilarity", "scvi-tools"]
dev = ["pytest"]

[project.scripts]
focal = "focal.cli:main"

[project.urls]
Homepage = "https://github.com/martinghl/focal"
Repository = "https://github.com/martinghl/focal"
Issues = "https://github.com/martinghl/focal/issues"

[tool.setuptools.packages.find]
where = ["."]
include = ["focal*"]
